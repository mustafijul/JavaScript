const cars = new Array("bmw", "marecedes", "toyota")

// console.log(cars);
for(let car of cars) {
    console.log(car);  // will print each element in the array one by one.  // It's a more modern and efficient way to loop through arrays.  
}

